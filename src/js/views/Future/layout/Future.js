import React from 'react'

export default ({ title, content }) => (
  <div className="future-part">
    <div className="future-title">{title}</div>
    <div className="future-content">{content}</div>
  </div>
)
