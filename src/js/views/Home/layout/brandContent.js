import React from 'react'

export default () => (
  <div className="brand-text">
    Most of our clients find themselves with volumes of data and intuition
    that something can be done with it — but they have no starting point.
    We guide them towards turning that data into game-changing technologies.
  </div>
)
